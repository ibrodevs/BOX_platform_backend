# 👨‍💼 Настройка администратора Django на Render.com

## ✅ Автоматическое создание администратора

Ваше приложение настроено для **автоматического создания** суперпользователя при первом деплое!

---

## 🔧 Настройка переменных окружения

В настройках вашего Web Service на Render.com добавьте:

```
DJANGO_SUPERUSER_USERNAME = admin
DJANGO_SUPERUSER_EMAIL = admin@bivolboxing.com
DJANGO_SUPERUSER_PASSWORD = ваш_надежный_пароль
```

### ⚠️ ВАЖНО:
- **Обязательно** установите переменную `DJANGO_SUPERUSER_PASSWORD` с надежным паролем!
- Используйте сложный пароль (минимум 12 символов, буквы + цифры + спецсимволы)
- **НЕ** используйте пароль по умолчанию "admin123" в production!

---

## 🚀 Доступ к админ-панели

После успешного деплоя администратор будет доступен по адресу:

```
https://your-app-name.onrender.com/admin/
```

### Данные для входа (по умолчанию):
- **Логин:** admin
- **Email:** admin@bivolboxing.com  
- **Пароль:** значение из `DJANGO_SUPERUSER_PASSWORD`

---

## 🔄 Как это работает

1. При деплое запускается скрипт `build.sh`
2. Выполняется команда `python manage.py create_superuser_if_none_exists`
3. Команда проверяет, есть ли уже суперпользователи
4. Если нет - создается новый с данными из переменных окружения
5. Если есть - пропускает создание

---

## 🛠 Ручное создание администратора

Если по какой-то причине автоматическое создание не сработало, вы можете создать администратора вручную:

### Вариант 1: Через Render Shell

1. Зайдите в Dashboard вашего Web Service на Render.com
2. Перейдите в раздел **"Shell"** 
3. Выполните команду:
   ```bash
   python manage.py createsuperuser
   ```
4. Следуйте инструкциям для создания пользователя

### Вариант 2: Через нашу команду

В Render Shell выполните:
```bash
python manage.py create_superuser_if_none_exists
```

---

## 🔐 Изменение пароля администратора

Если нужно изменить пароль существующего админа:

1. Зайдите в Render Shell
2. Выполните:
   ```bash
   python manage.py changepassword admin
   ```
3. Введите новый пароль

Или через админ-панель Django:
1. Войдите как администратор
2. Перейдите в раздел "Users" 
3. Выберите пользователя "admin"
4. Нажмите "Change password"

---

## 📝 Создание дополнительных администраторов

### Через админ-панель:
1. Войдите как суперпользователь
2. Перейдите в "Users"
3. Нажмите "Add user"
4. После создания отметьте "Staff status" и "Superuser status"

### Через Shell:
```bash
python manage.py createsuperuser --username new_admin --email new_admin@example.com
```

---

## ✅ Проверка созданного администратора

После деплоя проверьте логи сборки на Render.com. Вы должны увидеть одно из сообщений:

✅ `Superuser "admin" created successfully!`  
или  
✅ `Superuser already exists`

---

## 🔍 Решение проблем

### Проблема: Не могу войти в админ-панель
- Проверьте, что переменная `DJANGO_SUPERUSER_PASSWORD` установлена в Render
- Убедитесь, что вы используете правильный пароль
- Проверьте логи деплоя на наличие ошибок

### Проблема: Страница /admin/ не открывается
- Убедитесь, что деплой завершился успешно
- Проверьте, что URL правильный: `https://your-app.onrender.com/admin/`
- Проверьте статус вашего Web Service

### Проблема: Суперпользователь не создался
1. Зайдите в Render Shell
2. Проверьте наличие суперпользователей:
   ```python
   python manage.py shell
   >>> from accounts.models import User
   >>> User.objects.filter(is_superuser=True).count()
   ```
3. Если 0 - создайте вручную через `createsuperuser`

---

## 📚 Полезные команды Django

```bash
# Создать суперпользователя
python manage.py createsuperuser

# Изменить пароль пользователя
python manage.py changepassword username

# Список всех суперпользователей
python manage.py shell -c "from accounts.models import User; print(User.objects.filter(is_superuser=True).values_list('username', 'email'))"

# Сделать существующего пользователя администратором
python manage.py shell -c "from accounts.models import User; u = User.objects.get(username='USERNAME'); u.is_staff = True; u.is_superuser = True; u.save()"
```

---

## 🎯 Что дальше?

После входа в админ-панель вы сможете:
- ✅ Управлять пользователями
- ✅ Создавать и редактировать курсы
- ✅ Управлять уроками и видео
- ✅ Просматривать заказы и платежи
- ✅ Настраивать AI Coach
- ✅ Модерировать контент

---

**🔒 Безопасность:** Никогда не храните пароли в коде! Всегда используйте переменные окружения.
