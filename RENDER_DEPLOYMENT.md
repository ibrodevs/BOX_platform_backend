# 🚀 Инструкция по деплою на Render.com

## Подготовка завершена! ✅

Backend готов к деплою на Render.com. Все необходимые файлы созданы и настроены.

---

## 📋 Что было сделано:

### 1. **Обновлены зависимости** (`requirements.txt`)
- ✅ Добавлен `gunicorn` - WSGI сервер для production
- ✅ Добавлен `whitenoise` - для обслуживания статических файлов
- ✅ Добавлен `dj-database-url` - для работы с PostgreSQL на Render

### 2. **Создан скрипт сборки** (`build.sh`)
- Автоматическая установка зависимостей
- Сбор статических файлов
- Выполнение миграций базы данных

### 3. **Создан конфиг Render** (`render.yaml`)
- Настроена конфигурация web-сервиса
- Настроена PostgreSQL база данных
- Определены переменные окружения

### 4. **Обновлен `settings.py`**
- ✅ Настроено автоматическое определение production/development
- ✅ Добавлена поддержка PostgreSQL через DATABASE_URL
- ✅ Настроен WhiteNoise для статических файлов
- ✅ Добавлены настройки безопасности для production
- ✅ Настроен CORS для production доменов

---

## 🔧 Шаги для деплоя:

### Вариант 1: Через Dashboard (Рекомендуется)

1. **Создайте аккаунт на Render.com**
   - Зайдите на https://render.com
   - Зарегистрируйтесь через GitHub (рекомендуется)

2. **Загрузите код на GitHub**
   ```bash
   cd /Users/Apple/Desktop/projects/Новая\ папка/boxer-platform
   git init
   git add .
   git commit -m "Initial commit - Ready for Render deployment"
   # Создайте репозиторий на GitHub и загрузите код
   ```

3. **Создайте Web Service на Render**
   - Нажмите **"New +"** → **"Web Service"**
   - Подключите ваш GitHub репозиторий
   - Настройте следующие параметры:

   **Build Command:**
   ```bash
   cd backend && chmod +x build.sh && ./build.sh
   ```

   **Start Command:**
   ```bash
   cd backend && gunicorn boxer_platform.wsgi:application
   ```

   **Environment:**
   - Python 3

   **Plan:**
   - Free (или выберите платный план)

4. **Создайте PostgreSQL Database**
   - Нажмите **"New +"** → **"PostgreSQL"**
   - Выберите регион (тот же что и web service)
   - Имя: `boxer-platform-db`
   - Plan: Free
   - Нажмите **"Create Database"**

5. **Настройте Environment Variables в Web Service:**
   ```
   SECRET_KEY = [сгенерируйте случайную строку 50+ символов]
   DEBUG = False
   DATABASE_URL = [скопируйте Internal Database URL из PostgreSQL]
   ALLOWED_HOSTS = your-app-name.onrender.com
   CORS_ALLOWED_ORIGINS = https://your-frontend-domain.com,https://your-frontend-domain.vercel.app
   
   # Опционально (для AI и платежей):
   GEMINI_API_KEY = your_gemini_api_key
   STRIPE_SECRET_KEY = your_stripe_secret_key
   STRIPE_PUBLISHABLE_KEY = your_stripe_publishable_key
   ```

6. **Деплой!**
   - Render автоматически начнет деплой
   - Дождитесь завершения (3-5 минут)
   - Ваш API будет доступен по адресу: `https://your-app-name.onrender.com`

---

### Вариант 2: Через Blueprint (render.yaml)

1. **Загрузите код на GitHub** (см. выше)

2. **Создайте New Blueprint Instance**
   - Нажмите **"New +"** → **"Blueprint"**
   - Подключите репозиторий
   - Render автоматически найдет `render.yaml`
   - Настройте переменные окружения
   - Нажмите **"Apply"**

---

## 🔐 Генерация SECRET_KEY

Используйте один из способов:

**Python:**
```python
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

**Онлайн:**
```
https://djecrety.ir/
```

---

## 📝 Настройка переменных окружения

### Обязательные:
- `SECRET_KEY` - Django secret key (50+ символов)
- `DEBUG` - Всегда `False` в production
- `DATABASE_URL` - Автоматически из PostgreSQL (Internal URL)
- `ALLOWED_HOSTS` - Ваш домен на Render (например: `boxer-platform.onrender.com`)
- `CORS_ALLOWED_ORIGINS` - URL фронтенда (через запятую)

### Опциональные:
- `GEMINI_API_KEY` - Для AI-чата
- `STRIPE_SECRET_KEY` - Для платежей
- `STRIPE_PUBLISHABLE_KEY` - Для платежей

---

## 🧪 Проверка работы API

После успешного деплоя проверьте:

```bash
# Проверка health check
curl https://your-app-name.onrender.com/api/

# Проверка админки
https://your-app-name.onrender.com/admin/

# Проверка API endpoints
https://your-app-name.onrender.com/api/courses/
https://your-app-name.onrender.com/api/accounts/register/
```

---

## 🔄 Автоматический деплой

Render автоматически делает редеплой при каждом push в main ветку GitHub.

Чтобы отключить авто-деплой:
- Settings → Build & Deploy → Auto-Deploy: `No`

---

## 📊 Мониторинг

После деплоя доступны:
- **Logs** - в реальном времени
- **Metrics** - использование ресурсов
- **Events** - история деплоев

---

## ⚠️ Важные замечания:

1. **Free Plan ограничения:**
   - Сервис засыпает после 15 минут бездействия
   - Первый запрос может занять 30-60 секунд (cold start)
   - 750 часов в месяц бесплатно
   - База данных удаляется через 90 дней на Free плане

2. **Статические файлы:**
   - Обрабатываются через WhiteNoise
   - Автоматически сжимаются и кешируются

3. **Media файлы:**
   - На Free плане могут быть удалены при редеплое
   - Для постоянного хранения используйте AWS S3 или Cloudinary

4. **База данных:**
   - Используйте Internal Database URL для подключения
   - Делайте бэкапы регулярно

---

## 🔧 Дополнительная настройка (опционально)

### Custom Domain:
```
Settings → Custom Domain → Add your domain
```

### Health Check:
```python
# В urls.py добавьте простой endpoint:
path('health/', lambda request: JsonResponse({'status': 'ok'}))
```

### Scheduled Jobs (Cron):
```
Dashboard → Cron Jobs → Add Cron Job
Command: cd backend && python manage.py your_command
Schedule: @daily
```

---

## 🐛 Troubleshooting

### Ошибка при сборке:
```bash
# Проверьте логи сборки в Render Dashboard
# Убедитесь что build.sh имеет права на выполнение
chmod +x backend/build.sh
git add backend/build.sh
git commit -m "Fix build.sh permissions"
git push
```

### Ошибка DATABASE_URL:
```
# Убедитесь что используете Internal Database URL, а не External
# Internal URL format: postgresql://user:password@host/database
```

### Ошибка CORS:
```
# Добавьте домен фронтенда в CORS_ALLOWED_ORIGINS
# Формат: https://your-domain.com (без слэша в конце)
```

### Static files не работают:
```bash
# Выполните вручную:
cd backend && python manage.py collectstatic --no-input
```

---

## 📚 Полезные ссылки:

- Render Docs: https://render.com/docs
- Django Deployment: https://docs.djangoproject.com/en/4.2/howto/deployment/
- WhiteNoise: https://whitenoise.evans.io/
- Render Community: https://community.render.com/

---

## ✅ Чек-лист перед деплоем:

- [ ] Код загружен на GitHub
- [ ] `requirements.txt` обновлен
- [ ] `build.sh` создан и имеет права на выполнение
- [ ] `settings.py` настроен для production
- [ ] `.env` НЕ загружен в GitHub (добавлен в .gitignore)
- [ ] PostgreSQL база данных создана на Render
- [ ] Все переменные окружения настроены
- [ ] SECRET_KEY сгенерирован и добавлен
- [ ] DEBUG установлен в False
- [ ] ALLOWED_HOSTS содержит ваш домен
- [ ] CORS_ALLOWED_ORIGINS содержит домен фронтенда

---

**🎉 Готово! Ваш backend готов к деплою на Render.com!**

Если возникнут вопросы - проверьте логи в Render Dashboard или документацию выше.
